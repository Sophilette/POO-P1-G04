<?xml version="1.0" encoding="utf-8"?>
<!-- Card para Proveedor-->
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    card_view:cardBackgroundColor="#f4f6fc"
    card_view:cardCornerRadius="12dp"
    card_view:cardElevation="4dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- ID -->
        <TextView
            android:id="@+id/tvProvId"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="P001"
            android:textColor="#FF0D1D3D"
            android:textSize="16sp"
            android:textStyle="bold" />

        <!-- Nombre -->
        <TextView
            android:id="@+id/tvProvNombre"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:text="Repuestos Andina"
            android:textColor="#000000" />

        <!-- Teléfono -->
        <TextView
            android:id="@+id/tvProvTelefono"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:text="022345678"
            android:textColor="#000000" />

        <!-- Descripción -->
        <TextView
            android:id="@+id/tvProvDescripcion"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:text="Repuestos originales y alternos"
            android:textColor="#000000" />

        <!-- No usar en este avance -->
        <Button
            android:id="@+id/btndetalles"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:backgroundTint="#FF0D1D3D"
            android:text="Editar"
            android:textAllCaps="false"
            android:textColor="@android:color/white"
            android:visibility="gone" />

    </LinearLayout>
</androidx.cardview.widget.CardView>
